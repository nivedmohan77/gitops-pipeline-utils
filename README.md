# gitops-pipeline-utils
This script bridges the gap between your build pipeline (e.g., GitHub Actions, GitLab CI, or Azure Pipelines) and your deployment manifests. It dynamically parses target YAML files and replaces placeholder values with the newly built artifact metadata.
